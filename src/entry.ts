// ==============================================================================
// entry.ts — Runnable TypeScript CLI entry for Bun and Node (via tsx).
// Production installs still use bin/fm-server.js → dist/cli/main.js.
// ==============================================================================

import "./cli/main.js";
